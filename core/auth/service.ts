import {createHash,randomBytes} from "node:crypto";
export interface Account{id:string;handle:string;createdAt:string} export interface Session{token:string;account:Account;expiresAt:string}
export class AuthService {private accounts=new Map<string,{account:Account;digest:string}>();private sessions=new Map<string,Session>(); register(handle:string,password:string){if(!/^[a-zA-Z0-9][a-zA-Z0-9_-]{2,31}$/.test(handle))throw Error("Invalid handle");if(password.length<12)throw Error("Password must be at least 12 characters");const k=handle.toLowerCase();if(this.accounts.has(k))throw Error("Handle already taken");const account={id:randomBytes(16).toString("hex"),handle:k,createdAt:new Date().toISOString()};this.accounts.set(k,{account,digest:this.hash(password)});return this.newSession(account)} login(handle:string,password:string){const r=this.accounts.get(handle.toLowerCase());if(!r||r.digest!==this.hash(password))throw Error("Invalid handle or password");return this.newSession(r.account)} authenticate(token:string){const s=this.sessions.get(token);return s&&new Date(s.expiresAt)>new Date()?s.account:undefined} private newSession(account:Account){const s={token:randomBytes(32).toString("base64url"),account,expiresAt:new Date(Date.now()+3600000).toISOString()};this.sessions.set(s.token,s);return s} private hash(v:string){return createHash("sha256").update(v).digest("hex")} }
