; Build/link probe only. Never upload this in place of RIC firmware.
; Same classic ESP32/Arduino platform as firmware/esp32-pos.
[env:esp32dev]
platform = espressif32 @ ^6.4.0
board = esp32dev
framework = arduino
build_flags = -I ../../../firmware/esp32-pos/src
